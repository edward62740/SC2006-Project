<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="12dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_gravity="center">

        <ImageButton
            android:id="@+id/menu_item_image"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_gravity="center"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_launcher_foreground"/> <!-- Placeholder for actual image -->

        <EditText
            android:id="@+id/text_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:inputType="textMultiLine"
            android:hint="Menu Item Name"
            android:textAlignment="textStart"
            android:textColor="#AEEEEE"
            android:textSize="18sp"
            android:textStyle="bold"
            android:layout_marginBottom="4dp"/>

    </LinearLayout>

    <!-- Menu Details Container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="8dp">

        <!-- Menu Item Name -->


        <!-- Menu Item Description -->
        <EditText
            android:id="@+id/text_description"
            android:inputType="textMultiLine"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:hint="Description"
            android:textColor="#AEEEEE"
            android:padding="8dp"
            android:textSize="14sp"
            android:layout_weight="4"/>

        <!-- Price and Calories Horizontal Layout -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_weight="1">

            <!-- Menu Item Price -->
            <EditText
                android:id="@+id/text_price"
                android:inputType="numberDecimal"
                android:layout_width="100dp"
                android:layout_height="45dp"
                android:hint="$00.00"
                android:padding="8dp"
                android:textColor="#FFFFFF"
                android:textSize="16sp"
                android:textStyle="bold" />

            <!-- Spacer between price and calories -->
            <Space
                android:layout_width="match_parent"
                android:layout_height="10dp" />

            <EditText
                android:id="@+id/text_calories"
                android:inputType="number"
                android:layout_width="100dp"
                android:layout_height="45dp"
                android:layout_gravity="center"
                android:layout_marginStart="8dp"
                android:layout_marginEnd="8dp"
                android:textAlignment="center"
                android:hint="Calories"
                android:textColor="#AEEEEE"
                android:textSize="16sp"
                android:padding="8dp" />

         </LinearLayout>
    </LinearLayout>


</LinearLayout>


<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1D1D1D"
    android:orientation="vertical"
    android:padding="16dp">
    <TextView
        android:id="@+id/vendor_restaurants_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="16dp"
        android:text="Your Restaurants"
        android:textColor="#FFFFFF"
        android:textSize="28sp"
        android:textStyle="bold" />

    <!-- RecyclerView for the list of claimed stores -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewClaimedStores"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="#252525"
        android:scrollbars="vertical"
        android:padding="12dp"/>

    <!-- Button to claim a new store -->
    <Button
        android:id="@+id/claimNewStoreButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="64dp"
        android:backgroundTint="#00C853"
        android:text="Claim a new store"
        android:textColor="#FFFFFF"
        android:padding="12dp"/>

</LinearLayout>
